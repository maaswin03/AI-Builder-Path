You are a senior backend developer.

Create a RESTful Employee Management API using Python Flask.

Requirements:

* Implement CRUD operations (Create, Read, Update, Delete).
* Use Flask.
* Store employee data in memory using a Python list (no database).
* Employee fields: id, name, department, salary.
* Include proper HTTP status codes.
* Validate input data.
* Handle invalid requests gracefully.
* Organize the code clearly with comments where necessary.

Generate complete, executable code.
